---
import MainLayout from "../layouts/MainLayout.astro";
import Hero from "../components/Hero.astro";
import IndustriesSection from "../components/IndustriesSection.astro";
import ServicesGrid from "../components/ServicesGrid.astro";
import About from "../components/About.astro";
import Contacto from "../components/react/Contacto";
import MarcasCarousel from "../components/react/MarcasCarousel";
---

<MainLayout
  title="CompaniesAll - Desarrollo y Mantenimiento de Sistemas"
  description="Expertos en desarrollo y mantenimiento de sistemas para múltiples industrias. Soluciones tecnológicas a medida para tu empresa."
>
  <Hero
    title="Soluciones Tecnológicas para Impulsar tu Empresa"
    subtitle="Desarrollo y mantenimiento de sistemas a medida para múltiples industrias. Optimizamos tus procesos con tecnología de vanguardia."
    primaryButton="Nuestros Servicios"
    primaryLink="/servicios"
    secondaryButton="Soluciones por Industria"
    secondaryLink="/soluciones"
  />
  <About />

  <!-- <section id="servicios" class="section">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">Nuestros Servicios</h2>
        <p class="section-subtitle">
          Soluciones tecnológicas integrales para potenciar tu negocio
        </p>
      </div>
      <ServicesGrid />
    </div>
  </section> -->

  <section id="contacto">
    <Contacto client:load />
    <div class="container">
      <div class="cta-content"></div>
    </div>
  </section>

  <section id="contacto">
    <MarcasCarousel client:only="react" />
    <div class="container">
      <div class="cta-content"></div>
    </div>
  </section>
</MainLayout>

<style>
  :root {
    --primary-blue: #007aff; /* Azul de marca */
    --primary-blue-dark: #0056b3;
    --bg-light: #f9fafb;
    --text-dark: #1c1c1e;
    --text-medium: #8e8e93;
    --white: #ffffff;
    --shadow-light: 0 4px 15px rgba(0, 0, 0, 0.08);
    --shadow-medium: 0 6px 20px rgba(0, 0, 0, 0.12);
  }

  body {
    font-family: "Inter", sans-serif;
    color: var(--text-dark);
  }

  .section {
    padding: 8rem 0; /* Más espacio */
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem; /* Más padding en los lados */
  }

  .section-header {
    text-align: center;
    margin-bottom: 5rem; /* Más espacio */
  }

  .section-title {
    font-size: 3rem; /* Títulos más grandes y elegantes */
    font-weight: 700;
    color: var(--text-dark);
    margin-bottom: 1rem;
    letter-spacing: -0.05rem; /* Espaciado ajustado para estilo "Mac" */
  }

  .section-subtitle {
    font-size: 1.25rem;
    color: var(--text-medium);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.5;
  }

  .cta-section {
    background: linear-gradient(
      135deg,
      var(--primary-blue) 0%,
      var(--primary-blue-dark) 100%
    );
    color: white;
    text-align: center;
  }

  .cta-content {
    max-width: 700px;
    margin: 0 auto;
  }

  .cta-content h2 {
    color: white;
    margin-bottom: 1rem;
    font-size: 2.5rem;
    font-weight: 600;
  }

  .cta-content p {
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 2rem;
    font-size: 1.1rem;
  }

  .btn {
    border-radius: 50px; /* Estilo de botón de cápsula */
    padding: 0.9rem 2.5rem;
    font-size: 1.1rem;
    font-weight: 500;
    transition: all 0.2s ease-in-out;
  }

  .btn-primary {
    background-color: var(--white);
    color: var(--primary-blue);
    box-shadow: 0 5px 20px rgba(0, 0, 0, 0.15);
    border: none;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
  }

  /* Estilos del Hero */
  .hero {
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 5rem 1rem;
    background: linear-gradient(
      135deg,
      rgba(245, 247, 250, 0.9) 0%,
      rgba(255, 255, 255, 0.95) 100%
    );
    position: relative;
    overflow: hidden;
  }

  /* Fondo de patrones sutiles (pseudo-elemento) */
  .hero::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('data:image/svg+xml;utf8,<svg width="100%" height="100%" viewBox="0 0 1440 810" fill="none" xmlns="http://www.w3.org/2000/svg"><path opacity="0.05" d="M1440 0L0 810H1440V0Z" fill="url(%23paint0_linear_28_27)"/><path opacity="0.05" d="M0 0H1440V810L0 0Z" fill="url(%23paint1_linear_28_27)"/><circle opacity="0.05" cx="300" cy="500" r="100" fill="url(%23paint2_radial_28_27)"/><circle opacity="0.05" cx="1100" cy="200" r="150" fill="url(%23paint3_radial_28_27)"/><defs><linearGradient id="paint0_linear_28_27" x1="1440" y1="0" x2="0" y2="810" gradientUnits="userSpaceOnUse"><stop stop-color="#007AFF"/><stop offset="1" stop-color="#FFFFFF"/></linearGradient><linearGradient id="paint1_linear_28_27" x1="0" y1="0" x2="1440" y2="810" gradientUnits="userSpaceOnUse"><stop stop-color="#007AFF"/><stop offset="1" stop-color="#FFFFFF"/></linearGradient><radialGradient id="paint2_radial_28_27" cx="0" cy="0" r="1" gradientUnits="userSpaceOnUse" gradientTransform="translate(300 500) rotate(90) scale(100)"><stop stop-color="#007AFF"/><stop offset="1" stop-color="#FFFFFF" stop-opacity="0"/></radialGradient><radialGradient id="paint3_radial_28_27" cx="0" cy="0" r="1" gradientUnits="userSpaceOnUse" gradientTransform="translate(1100 200) rotate(90) scale(150)"><stop stop-color="#007AFF"/><stop offset="1" stop-color="#FFFFFF" stop-opacity="0"/></radialGradient></defs></svg>');
    background-size: cover;
    background-position: center;
    z-index: 1;
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
    text-align: center;
    padding: 2rem;
  }

  .hero-content h1 {
    font-family: "Inter", sans-serif;
    font-size: 3.8rem;
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--text-dark);
    letter-spacing: -0.06em;
  }

  .hero-content p {
    font-family: "Inter", sans-serif;
    font-size: 1.3rem;
    line-height: 1.6;
    color: var(--text-medium);
  }

  .hero-buttons {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-top: 3rem;
  }

  /* Estilos de botones estilo macOS */
  .hero-buttons a {
    text-decoration: none;
    font-family: "Inter", sans-serif;
    font-weight: 600;
    padding: 0.8rem 2.2rem;
    border-radius: 50px;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
  }

  .primary-button {
    background: var(--primary-blue);
    color: var(--white);
    border: 1px solid var(--primary-blue);
  }

  .primary-button:hover {
    background: var(--primary-blue-dark);
    border-color: var(--primary-blue-dark);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
  }

  .secondary-button {
    background: var(--white);
    color: var(--primary-blue);
    border: 1px solid var(--primary-blue);
    /* Efecto de cristal */
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }

  .secondary-button:hover {
    background: rgba(240, 248, 255, 0.8);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .section-title {
      font-size: 2.2rem;
    }
    .hero-buttons {
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }
    .hero-content h1 {
      font-size: 2.5rem;
    }
  }
</style>
