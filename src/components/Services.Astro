---
// components/Servicios.astro
---

<section id="servicios" class="servicios-section" aria-labelledby="servicios-title">
  <div class="container">
    <div class="section-header" data-animate="fade-in">
      <h2 id="servicios-title" class="section-title">Nuestros Servicios</h2>
      <p class="section-subtitle">Soluciones digitales que impulsan tu negocio</p>
    </div>

    <div class="servicios-grid" data-animate="stagger">
      <article class="service-card">
        <div class="service-icon" aria-hidden="true">💻</div>
        <h3 class="service-title">Desarrollo Web</h3>
        <p class="service-description">Creamos sitios web y aplicaciones a medida que son rápidas, seguras y escalables.</p>
      </article>

      <article class="service-card">
        <div class="service-icon" aria-hidden="true">📱</div>
        <h3 class="service-title">Desarrollo Mobile</h3>
        <p class="service-description">Diseñamos y desarrollamos aplicaciones móviles para iOS y Android con la mejor experiencia de usuario.</p>
      </article>

      <article class="service-card">
        <div class="service-icon" aria-hidden="true">☁️</div>
        <h3 class="service-title">Infraestructura</h3>
        <p class="service-description">Configuramos y gestionamos la infraestructura en la nube para garantizar un rendimiento óptimo.</p>
      </article>

      <article class="service-card">
        <div class="service-icon" aria-hidden="true">🔧</div>
        <h3 class="service-title">Servicio Técnico</h3>
        <p class="service-description">Ofrecemos soporte y mantenimiento para asegurar el correcto funcionamiento de tus sistemas.</p>
      </article>

      <article class="service-card">
        <div class="service-icon" aria-hidden="true">🎓</div>
        <h3 class="service-title">Capacitaciones</h3>
        <p class="service-description">Capacitamos a tu equipo en las últimas tecnologías y herramientas digitales.</p>
      </article>
    </div>
  </div>
</section>

<style>
  /* Variables CSS optimizadas */
  :root {
    --primary: #007bff;
    --primary-dark: #0056b3;
    --accent: #10b981;
    --text-dark: #1a1a1a;
    --text-medium: #666;
    --text-light: #888;
    --white: #ffffff;
    --bg-light: #f9fafb;
    --bg-card: #ffffff;
    --shadow-light: 0 2px 8px rgba(0, 0, 0, 0.04);
    --shadow-medium: 0 8px 30px rgba(0, 0, 0, 0.08);
    --shadow-hover: 0 12px 40px rgba(0, 123, 255, 0.15);
    --radius: 16px;
    --transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  /* Estilos base optimizados */
  .servicios-section {
    padding: clamp(3rem, 8vw, 5rem) 0;
    background: var(--bg-light);
    
    position: relative;
  }

  .container {
    max-width: min(1200px, 90vw);
    margin: 0 auto;
    padding: 0 clamp(1rem, 3vw, 2rem);
  }

  /* Header de sección */
  .section-header {
    text-align: center;
    margin-bottom: clamp(2rem, 5vw, 3rem);
  }
  
  .section-title {
    font-size: clamp(2rem, 5vw, 2.5rem);
    font-weight: 700;
    color: var(--text-dark);
    line-height: 1.2;
    margin-bottom: 1rem;
  }

  .section-subtitle {
    font-size: clamp(1rem, 2.5vw, 1.1rem);
    color: var(--text-medium);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.6;
  }

  /* Grid de servicios */
  .servicios-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(300px, 100%), 1fr));
    gap: clamp(1.5rem, 3vw, 2rem);
    justify-content: center;
  }

  /* Tarjetas de servicio */
  .service-card {
    background: var(--bg-card);
    padding: clamp(2rem, 4vw, 2.5rem);
    border-radius: var(--radius);
    text-align: center;
    box-shadow: var(--shadow-light);
    transition: var(--transition);
    border: 1px solid transparent;
    position: relative;
    overflow: hidden;
    will-change: transform, box-shadow;
  }

  .service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(0, 123, 255, 0.03), transparent);
    transition: left 0.6s ease;
  }

  .service-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-hover);
    border-color: rgba(0, 123, 255, 0.1);
  }

  .service-card:hover::before {
    left: 100%;
  }

  .service-icon {
    font-size: clamp(2.5rem, 6vw, 3rem);
    margin-bottom: clamp(1rem, 2vw, 1.5rem);
    display: block;
    transition: var(--transition);
    filter: grayscale(0.3);
  }

  .service-card:hover .service-icon {
    transform: scale(1.1);
    filter: grayscale(0);
  }

  .service-title {
    font-size: clamp(1.25rem, 3vw, 1.5rem);
    font-weight: 600;
    color: var(--text-dark);
    margin-bottom: clamp(0.75rem, 2vw, 1rem);
    line-height: 1.3;
  }

  .service-description {
    font-size: clamp(0.9rem, 2vw, 1rem);
    color: var(--text-medium);
    line-height: 1.6;
    margin: 0;
  }

  /* Animaciones optimizadas */
  @media (prefers-reduced-motion: no-preference) {
    [data-animate] {
      opacity: 0;
      transform: translateY(30px);
      transition: 
        opacity 0.8s cubic-bezier(0.4, 0, 0.2, 1),
        transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    [data-animate].animated {
      opacity: 1;
      transform: translateY(0);
    }
    
    /* Animación escalonada para las tarjetas */
    [data-animate="stagger"].animated .service-card {
      animation: cardSlideUp 0.6s ease-out forwards;
    }
    
    [data-animate="stagger"].animated .service-card:nth-child(1) { animation-delay: 0.1s; }
    [data-animate="stagger"].animated .service-card:nth-child(2) { animation-delay: 0.2s; }
    [data-animate="stagger"].animated .service-card:nth-child(3) { animation-delay: 0.3s; }
    [data-animate="stagger"].animated .service-card:nth-child(4) { animation-delay: 0.4s; }
    [data-animate="stagger"].animated .service-card:nth-child(5) { animation-delay: 0.5s; }
    
    @keyframes cardSlideUp {
      from {
        opacity: 0;
        transform: translateY(30px) scale(0.95);
      }
      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .service-card,
    .service-icon {
      transition: none;
    }
    
    .service-card:hover {
      transform: none;
    }
    
    [data-animate] {
      transition: none;
      animation: none;
      opacity: 1;
      transform: none;
    }
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .servicios-section {
      padding: clamp(2rem, 6vw, 3rem) 0;
    }
    
    .servicios-grid {
      grid-template-columns: 1fr;
      max-width: 400px;
      margin: 0 auto;
    }
    
    .service-card {
      padding: 1.5rem;
    }
  }

  @media (max-width: 480px) {
    .service-card {
      padding: 1.25rem;
    }
    
    .service-icon {
      font-size: 2.5rem;
    }
    
    .service-title {
      font-size: 1.2rem;
    }
  }

  /* Mejoras de accesibilidad */
  @media (prefers-contrast: high) {
    .service-card {
      border: 2px solid var(--primary);
    }
    
    .service-card:hover {
      border-color: var(--primary-dark);
    }
  }

  /* Focus states para accesibilidad */
  .service-card:focus-within {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }

  /* Modo oscuro */
  @media (prefers-color-scheme: dark) {
    :root {
      --text-dark: #ffffff;
      --text-medium: #a0a0a0;
      --bg-light: #1a1a1a;
      --bg-card: #2a2a2a;
      --shadow-light: 0 2px 8px rgba(0, 0, 0, 0.2);
      --shadow-medium: 0 8px 30px rgba(0, 0, 0, 0.3);
    }
  }
</style>

<script>
  // Script optimizado sin errores
  document.addEventListener('DOMContentLoaded', function() {
    'use strict';
    
    // Configuración del Intersection Observer
    var observerOptions = {
      root: null,
      rootMargin: '50px',
      threshold: 0.15
    };
    
    var observer;
    
    // Verificar si el usuario prefiere movimiento reducido
    var prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    
    if (!prefersReducedMotion) {
      observer = new IntersectionObserver(function(entries) {
        entries.forEach(function(entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('animated');
            observer.unobserve(entry.target);
          }
        });
      }, observerOptions);
      
      // Observar elementos animados
      var animatedElements = document.querySelectorAll('[data-animate]');
      animatedElements.forEach(function(el) {
        observer.observe(el);
      });
    } else {
      // Si prefiere movimiento reducido, mostrar todo inmediatamente
      var animatedElements = document.querySelectorAll('[data-animate]');
      animatedElements.forEach(function(el) {
        el.classList.add('animated');
      });
    }
    
    // Mejorar interacción de las tarjetas
    var serviceCards = document.querySelectorAll('.service-card');
    
    serviceCards.forEach(function(card) {
      // Feedback táctil para móviles
      card.addEventListener('touchstart', function() {
        this.style.transform = 'translateY(-2px) scale(0.99)';
      }, { passive: true });
      
      card.addEventListener('touchend', function() {
        this.style.transform = '';
      }, { passive: true });
      
      // Prevenir múltiples clics rápidos
      card.addEventListener('click', function(event) {
        if (this.classList.contains('clicked')) {
          event.preventDefault();
          return;
        }
        
        this.classList.add('clicked');
        setTimeout(function() {
          this.classList.remove('clicked');
        }.bind(this), 1000);
      });
    });
    
    // Cleanup para evitar memory leaks
    window.addEventListener('beforeunload', function() {
      if (observer) {
        observer.disconnect();
      }
    });
  });
</script>